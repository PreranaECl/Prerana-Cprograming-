1.	Write a C Program to check whether the user-entered integer is a positive number or a negative number.
  
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a;
    printf("enter the number: \n");
    scanf("%d",&a);
    if(a>0)
    {
        printf("the number %d is positive",a);
    }
    else
    {
        printf("the given number %d is negative",a);
    }
    return 0;
}

2.	Write a C Program to check whether the user-entered two numbers are equal or not.
  
#include <stdio.h>
int main() {
int a,b;
printf("enter two numbers:");
scanf("%d%d",&a,&b);
if(a==b)
printf("the numbers are equal");
else
printf("the numbers are not equal");
return 0;
}

3.	Write a C Program to check whether the user-entered integer is a digit or a number.
4.	Write a C Program to check whether the user-entered integer is a two-digit number or not.
  
#include <stdio.h>
int main()
{
int d;
printf("enter the integer:");
scanf("%d",&d);
if(d>=-99 && d<=99)
printf("%d is a two  digit",d);
else
printf("%d is not a two  digit",d);
return 0;
}

5.	Write a C Program to check whether the user-entered number is a multiple of 3 and 5 or not.
  
#include <stdio.h>
int main() {
int n;
printf("Enter the number");
scanf("%d",&n);
if(n%3==0 && n%5==0)
printf("%d is a multiple of 3 and 5",n);
else
printf("%d is not a multiple of 3 and 5",n);
return 0;
}

6.	Write a C Program to check whether the month number is a valid month number or an invalid month number.
  
#include <stdio.h>
int main() {
    int month;
    printf("Enter a month number: ");
    scanf("%d", &month);
    if (month >= 1 && month <= 12) {
        printf("%d is a valid month number.\n", month);
    } else {
        printf("%d is an invalid month number.\n", month);
    }
    return 0;
}

7.	Write a C Program to check whether the number is an even number or an odd number.
#include <stdio.h>

int main() {
    int number;
    printf("Enter an integer: ");
    scanf("%d", &number);
    if (number % 2 == 0) {
        printf("%d is even.\n", number);
    } else {
        printf("%d is odd.\n", number);
    }
    return 0;
}

8.	Write a C Program to check whether the user-entered number is a perfect square number or not.
#include <stdio.h>
#include <math.h>

int main() {
    int number;
    printf("Enter an integer: ");
    scanf("%d", &number);

    int sqrtValue = sqrt(number);
    if (sqrtValue * sqrtValue == number) {
        printf("%d is a perfect square.\n", number);
    } else {
        printf("%d is not a perfect square.\n", number);
    }

    return 0;
}

9.	Write a C Program to check if a person is eligible to vote or not based on their age.
#include <stdio.h>

int main()
{
int d;
printf("enter the integer:");
scanf("%d",&d);
if(d>18)
printf("the person is eligible");
else
printf("the person is not eligible");
return 0;
}

10.	Write a C Program to check if a character is a vowel or a consonant.
#include <stdio.h>
int main()
{
char d;
printf("enter the aplhabet:");
scanf("%c",&d);
if(d=='a'|| d=='e'|| d=='i'|| d=='o'|| d=='u')
printf("the letter is vowel");
else
printf("the letter is a consonant");
return 0;
}
